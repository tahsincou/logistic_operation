class ShipmentTimeline {
  final String title;
  final DateTime? time;
  final bool completed;

  const ShipmentTimeline({
    required this.title,
    required this.time,
    required this.completed,
  });
}
